server:
  port: 8083  # 👈 Aquí defines el nuevo puerto

spring:
  datasource:
    url: jdbc:oracle:thin:@localhost:1521:xe  # Ajusta esto si usas otro puerto o servicio
    username: ENUCOM
    password: AI243sj
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.OracleDialect